{
  "name": "telegsar-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "set:webhook": "node src/setWebhook.js",
    "pm2:start": "pm2 start npm --name telegsar-api -- start && pm2 save",
    "pm2:restart": "pm2 restart telegsar-api",
    "pm2:stop": "pm2 stop telegsar-api"
  },
  "dependencies": {
    "@prisma/client": "^6.14.0",
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "node-schedule": "^2.1.1"
  },
  "devDependencies": {
    "prisma": "^6.14.0"
  }
}
